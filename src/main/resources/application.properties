spring.application.name=FitZone

# JWT Configuration
jwt.secret=UniversidadDelQuindioSoftware3GarciaLopezValencia
jwt.expiration=3600
jwt.refreshExpiration=2592000

# SendGrid Configuration
sendgrid.api.key=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
sendgrid.from.email=fitzoneuq@gmail.com

# Database Configuration (Neon PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/fitzone
spring.datasource.username=postgres
spring.datasource.password=juanda4566
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Stripe Configuration
stripe.api.key.secret=${STRIPE_API_KEY_SECRET}

# Production optimizations
server.port=${PORT:8080}
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.defaults.enabled=true
management.health.diskspace.enabled=true

# Logging configuration
logging.level.org.springframework=INFO
logging.level.com.yourpackage=DEBUG
logging.file.name=./logs/fitzone-app.log
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
